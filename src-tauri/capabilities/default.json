{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for Keyplane app and overlay windows",
  "windows": ["main", "overlay"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
