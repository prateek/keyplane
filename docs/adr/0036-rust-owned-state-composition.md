# Rust-owned state composition

Rust will own source composition, Backend Health, Layer Stack resolution, and Effective Action derivation. The web frontend consumes Keyboard Snapshots plus Runtime Events and focuses on rendering, settings, Import Review, and Source Inspector UI rather than implementing protocol, HID, or keymap resolution logic.
