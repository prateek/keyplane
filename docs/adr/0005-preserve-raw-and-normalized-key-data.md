# Preserve raw and normalized key data

The keyboard model will keep both normalized display fields and source-specific raw action data. This lets the app render clean legends while preserving enough information for debugging, protocol-specific behavior, future parsing improvements, and possible export or round-trip workflows.
