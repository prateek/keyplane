# Firmware state is the primary source of truth

The app will treat firmware or remapper layer state as the primary source of truth, with host input events used as a secondary visualization stream. This keeps the app centered on real keyboard layers and physical positions rather than reducing it to a keyviz-style OS input display.
