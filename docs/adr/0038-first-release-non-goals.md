# First-release non-goals

The first release will not include arbitrary Kanata `.kbd` parsing, a full visual Profile editor, multi-keyboard active mode, a firmware flashing assistant, write-back to Vial/ZMK/QMK, a recent-input keyviz clone, or exact firmware behavior simulation. These exclusions keep the first product focused on live full-keyboard layer visualization, source composition, and import quality.
