# MVP acceptance criteria

The MVP is acceptable when one KeyPeek-backed keyboard shows live layer changes in a full-keyboard Overlay Window, one NocFree/Vial import renders as Best-Effort Preview, Profiles save and load as EDN, Backend Health appears in the UI, and the overlay is transparent, click-through, pinned, and positionable.
