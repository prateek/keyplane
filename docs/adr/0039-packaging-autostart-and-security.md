# Packaging, autostart, and security

The MVP can start as developer/local desktop builds, while keeping the design ready for signed desktop builds, launch-at-login, accessibility and input-monitoring prompts, minimal permissions, and visible permission health. Permission and autostart behavior should be modeled through Backend Health and app settings rather than hidden logs or platform-only assumptions.
