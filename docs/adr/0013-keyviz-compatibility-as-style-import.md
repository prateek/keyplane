# keyviz compatibility as style import

MVP keyviz compatibility means importing keyviz style JSON into the app's Visual Style model. The full-keyboard layer overlay will not be based on keyviz's recent-input visualizer structure, because host input history is a different Overlay Surface from rendering a keyboard's Physical Layout, Logical Keymap, and Runtime State.
