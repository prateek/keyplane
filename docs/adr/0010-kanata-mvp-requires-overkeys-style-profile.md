# Kanata MVP requires an OverKeys-style profile

The MVP Kanata backend will require an OverKeys-compatible companion profile rather than parsing arbitrary Kanata `.kbd` files. Kanata will provide runtime layer-change events, while the companion profile provides renderable layer data, labels, aliases, triggers, and connection settings.
