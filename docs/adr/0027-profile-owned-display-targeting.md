# Profile-owned display targeting

Display Targeting will be profile-specific, with a Global Display Fallback when the Active Profile has no saved target. Different keyboard workflows may need different monitor, placement, size, and opacity settings, so the MVP can store one active profile's targeting while keeping the model profile-owned.
