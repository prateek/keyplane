# OverKeys compatibility is import-only at first

OverKeys configuration compatibility will mean importing OverKeys row layouts, triggers, aliases, styling preferences, and Kanata connection settings into this app's model. The MVP will not write back to OverKeys config files, because the new model is richer and should not be constrained by OverKeys' row-array format.
