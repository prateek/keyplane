# Keyplane product identity

Use `Keyplane` as the initial product name and `keyplane` as the repo, crate, and package slug. This gives the fork a distinct identity from KeyPeek and OverKeys while fitting the app's layer-oriented model. This is an implementation default, not trademark clearance.
