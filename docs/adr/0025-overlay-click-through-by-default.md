# Overlay click-through by default

The Overlay Window will run in Click-Through Mode by default so it does not steal focus or intercept pointer input while the user types. A separate Positioning Mode will temporarily disable click-through behavior and show drag or resize affordances, then return the overlay to click-through rendering when positioning ends.
