# Dedicated overlay window

The keyboard overlay will be a dedicated transparent, frameless, always-on-top Overlay Window separate from the normal App Window. Overlay lifecycle, placement, opacity, click-through behavior, and display targeting should be owned by the overlay window so rendering does not fight profile, import, settings, or Source Inspector UI.
