# Public hand-editable EDN profiles

The app-native EDN Profile will be treated as a public, hand-editable configuration contract from the MVP. Profiles will carry a Profile Schema Version, use Stable Element IDs for keys, layers, and sources, save in deterministic formatting, and support migrations so users can read and edit profiles without relying on an opaque app database.
