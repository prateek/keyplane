# Full-keyboard layer overlay first

The first product surface will be a full-keyboard layer overlay, not a recent-input overlay. keyviz-style input visualization and keycap styling remain useful, but the initial product loop is seeing the connected keyboard's active layer and key meanings.
