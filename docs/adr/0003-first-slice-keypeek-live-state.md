# First slice uses KeyPeek live state

The first usable vertical slice will start from KeyPeek's existing firmware-module live-state path before adding lower-friction Vial/NocFree import flows. This gives the app a working end-to-end loop for layout import, active layer changes, and overlay rendering before expanding to devices that can provide keymap data but lack a live layer channel.
