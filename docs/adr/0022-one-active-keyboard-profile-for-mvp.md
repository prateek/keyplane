# One active keyboard profile for MVP

The MVP will support exactly one Active Profile at a time for live overlay behavior. The data model should not prevent multiple keyboards later, but simultaneous active devices are out of scope until the single-keyboard overlay, source precedence, backend health, and profile selection flows are solid.
