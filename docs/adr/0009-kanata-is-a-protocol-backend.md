# Kanata is a protocol backend

Kanata will plug into the same protocol backend abstraction as firmware-aware sources, reporting layer-change runtime state through the unified backend event stream. Because Kanata does not usually provide complete physical geometry, its keymap and layout data may need to come from a companion source such as an OverKeys-style profile.
