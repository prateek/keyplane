# Structured display legends

Display Legends will be structured slots, not a single label string. A key can expose primary, shifted or secondary, hold/tap-role, layer or action hint, and optional icon slots, each tied back to Raw Action Source Provenance; renderers may collapse those slots into a simple label for minimal Style Variants while preserving richer meaning for keyviz-style keycaps.
