# EDN profile sections

The app-native EDN Profile will have stable top-level sections for schema version, profile identity, sources, physical layout, keymap, runtime backends, visual style, overlay window, source precedence, user overrides, and source provenance. These sections give importers, hand-editing, migrations, and source inspection a predictable contract without adopting any external configuration format as canonical.
