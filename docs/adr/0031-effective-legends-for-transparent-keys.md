# Effective legends for transparent keys

Transparent keys will preserve their transparent Raw Action in the model while rendering the Effective Action the user will get from lower-layer inheritance. The overlay should display the effective Display Legend with a subtle inherited indicator or source hint, while the Source Inspector and Profile views show the actual transparent entry and the inherited source that supplied the displayed value.
