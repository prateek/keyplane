# App-native EDN profile format

Profiles will use an app-native EDN format as the canonical saved representation. KeyPeek, OverKeys, keyviz style JSON, Vial/VIA, ZMK, and Kanata companion data will enter through importers, with source provenance preserved, but none of those external formats will define the internal profile model.
